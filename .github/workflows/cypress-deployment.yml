name: PR via devops‑infra

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create Pull Request
        id: pr
        uses: devops-infra/action-pull-request@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          source_branch: main
          target_branch: newbranch
          title: 'Automated PR from main → newbranch'
          body: 'This PR was created by devops‑infra/action-pull-request in draft.'
          draft: true
          allow_no_diff: false

      - name: Show PR outputs
        run: |
          echo "📦 PR number: ${{ steps.pr.outputs.pr_number }}"
          echo "📦 PR URL:    ${{ steps.pr.outputs.url }}"
